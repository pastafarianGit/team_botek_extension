setTimeout(function() {
    const travian = window.Travian;
    let key  = travian.centipedeLiquefactionDecoratingCustodial;
    document.dispatchEvent(new CustomEvent('bearer_key_action', {
        detail: key
    }));
}, 1000);
